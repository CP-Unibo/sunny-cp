- class Solver:
    old Solver +
    id = '' # instead of name
    state = 'ready' # 'converting', 'solving', 'suspended'
    process = None # Popen(...)
    start_time = -1
    timeout = -1
    solution_time = -1
    solution = []
    obj_value = None
    options = ''
    
  --opt-<SOLVER_NAME> "OPTIONS" 
  --opt-all "OPTIONS"  (def "-a" if COP, "" if CSP)
  -a == --opt-all "-a" (def set if COP, not set if CSP)
  -f == --opt-all "-f" (def not set)
    
  RUNNING_SOLVERS + SUSPENDED_SOLVERS {id: solver}

- pre-solving + solving

- documentare

- testing (coverage?) 

- integrare installazione con mzn2feat (PB Makefile: var. d'ambiente)

- filtrare dataset, solvers, features x ridurre complessità
