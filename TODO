* RunningSolver ISA Solver

* Aggiungere nuovi parametri: --switch-search, -z per bound iniziale, 
  -X/-A for excluding/adding solver(s) from/to the default pfolio
  --max-restart X: max. no. of restarts allowed

* python setup.py install (rimuovere var d'ambiente nel codice) e integrazione 
  con mzn2feat

* velocizzare calcolo scedule
  
* MiniZinc 2.0 (anche mzn2feat) e nuovi solvers per mznc15

* Docker

* pytdoc + unit test (con code coverage)

* Release sunny-cp 2.1 prima di mznc15