%
% native support of all_diffrent
%

predicate all_different(array[int] of var int: x) = all_different_int(x);
predicate alldifferent(array[int] of var int: x) = all_different_int(x);

predicate all_different_int(array[int] of var int: x);
